---
description: Full feature implementation from database to UI
---

1. **Analyze Requirements**: Use `product-planner` and `create-roadmap` to define feature scope.
2. **Design Architecture**: Use `system-architect` to design schema and component structure.
// turbo
3. **Create Database Schema**: Use `supabase-expert` and `create-migration` to generate migrations.
// turbo
4. **Create Backend API**: Use `fullstack-developer` and `create-service` to implement data logic.
5. **Create React Context**: Use `react-specialist` and `create-context` for state management.
6. **Create components**: Use `react-specialist` and `create-component` for UI.
7. **Create Hooks**: Use `react-specialist` and `create-hook` for reusable logic.
8. **Add Tests**: Use `qa-engineer` to ensure coverage and reliability.
// turbo
9. **Validate Build**: Run `npm run build` to ensure project integrity.
